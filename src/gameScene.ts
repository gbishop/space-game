import "phaser";

// global game options
let gameOptions = {
};

export class GameScene extends Phaser.Scene {

  constructor() {
    super({
      key: "GameScene"
    });
  }

  init(): void {}

  preload(): void {
  }

  create(): void {
  }
  update() {
  }
}
