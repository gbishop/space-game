{
  "name": "Runner",
  "version": "0.1.0",
  "description": "Runner game (Phaser 3 + TypeScript)",
  "main": "webpack.config.js",
  "scripts": {
    "build": "webpack --mode production",
    "start": "webpack-dev-server --mode development --hot",
    "pages": "git checkout gh-pages; git merge --no-commit master; npm run build; git add dist/app.js; git commit -m 'update pages'; git push origin gh-pages; git checkout master"
  },
  "author": "Gary Bishop after Mariya Davydova",
  "license": "MIT",
  "devDependencies": {
    "phaser": "^3.15.1",
    "webpack": "^4.26.0",
    "webpack-cli": "^3.1.2",
    "webpack-dev-server": "^3.8.0"
  }
}
